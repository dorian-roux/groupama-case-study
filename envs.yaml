name: py38_groupama
channels:
  - defaults
dependencies:
  - python=3.8.15
  - pip
  - pip:
    # works for regular pip packages
    - pandas
    - wget
    - requests
    - xlrd
    - seaborn
    - matplotlib
    - scikit-learn